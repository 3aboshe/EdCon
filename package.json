{
    "name": "edcon-app",
    "private": true,
    "workspaces": [
        "admin",
        "server"
    ],
    "scripts": {
        "build": "npm run build --workspace=admin",
        "install": "npm install --workspace=admin"
    }
}